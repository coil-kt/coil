<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="coil">

    <application>
        <provider
            android:name=".util.CoilContentProvider"
            android:authorities="${applicationId}.coil"
            android:exported="false"/>
    </application>
</manifest>
